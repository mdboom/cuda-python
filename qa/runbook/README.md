# Runbook

This directory contains Linux and Windows build/test command scripts used as a
human-readable runbook for `ctk-next` release preparation.

The scripts are useful in two ways:

- direct use during pre-handover validation
- reference material for equivalent steps in the QA team's automation systems

This directory is updated in place across CTK release cycles.
